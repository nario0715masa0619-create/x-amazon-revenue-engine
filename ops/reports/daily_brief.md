# daily_brief.md — 日次ブリーフ

> **2026-08-07以降、このファイルは正本ではありません。** 正本はGoogle Sheetsの`posts`/`reviews`/`metrics_24h`（`ops-state` MCPサーバー経由でのみ読み書き。詳細: [mcp_architecture_2026-08-07.md](mcp_architecture_2026-08-07.md)）です。以後、このファイルは`render_daily_brief()`ツールが呼び出し時点の状態から**都度上書き生成するビュー**になります。**手編集しないでください**（次回生成時に失われます）。
>
> 以下は2026-08-02〜2026-08-06にかけて手動維持していた履歴です。移行時点でSheetsへ未バックフィルのため、**過去の記録として保持**しています（削除していません）。`render_daily_brief()`を実行すると、この内容は生成ビューに置き換わります。

> （旧テンプレート説明）mode-orchestratorが自動生成・維持するAI主導の記録。**人間が毎日ゼロから埋める運用ではない。** 当日方針の決定は`.claude/skills/morning-strategy-council/SKILL.md`（Morning Strategy Brief）が担い、このファイルはその後の実行結果・記録を控える場所に役割を絞る（[phase1_acquisition_launch_spec_2026-08-03.md](phase1_acquisition_launch_spec_2026-08-03.md)の「最小オペレーション標準フロー」参照）。人間の入力が必要な欄には都度「（人間が入力）」と明記し、それ以外は原則AI側が埋める。

## 対象日

2026-08-06

## 現在モード

- `ops/state/current_mode.yaml` の値: `acquisition`
- 直近のモード別投稿比率（実績）: 集客 100% / 教育 0% / 販売 0%（`post_log.jsonl`の実記録はすべてacquisitionのため）

## 前日の実績サマリ

- Day1（`p-20260803-001`）: impression 8、Day2（`p-20260804-002`）: impression 3（いずれもスクショ確認ベース、`data_quality: manual`相当の実測値）。**ただし、この2件はまだ`ops/logs/post_log.jsonl`・`ops/logs/metrics_snapshots.csv`・この表のいずれにも実データとして記録されていない未整合が残っている（既知のギャップ）。** 今回のログ整合作業では、旧Day3候補の棄却と本日の案A/案B判断のみを反映し、Day1/Day2実績の正式バックフィルは別途対応とする

## 本日の方針（Morning Strategy Briefからの転記）

- テーマ: T1（見た目を損ねない実用品）／target: 40代男性／format: `single_post`／CTA: `profile_visit`
- 角度・フック方向: 他者視線・気まずさ・仕事文脈（一貫した仮説のもと、複数の具体的場面で検証を継続）
- **2026-08-07 人間最終判断（候補を閉じる）**:
  - `p-20260807-002`（案D、充電器の貸し借り）: **次の本命候補として人間最終審査へ提示**
  - `p-20260806-001`（案A、会議室・ケーブル）: 承認済みだが**今回は優先しない保留**（優先度を下げる。棄却ではない）
  - `p-20260806-002`（案B、名刺交換・ペンケース）: 保留（変更なし）
  - `p-20260807-001`（案C、タクシー・ケーブル修正版）: `observation only`のまま保持（変更なし）
  - `p-20260805-001`（旧Day3候補、取引先前・ケーブル）: **失敗作として閉じる**（`human_rejected_meaning_unclear`、確定）
  - 新しい案は増やさない（今回は分岐を閉じるフェーズ）

## 実投稿記録（投稿完了後。**人間の入力は投稿URLのみ**）

承認済み候補（`post_log.jsonl`の`status: approved`）はpost_idが既に確定しているため、AI（logger）が行を事前に用意する。人間は**投稿URLを貼るだけ**でよい。投稿時刻はURL記入時点、投稿者は既定値（単独運用中は固定名）をAI側で補う。

| post_id | 投稿URL（人間が入力） | 投稿時刻（AI補完） | 投稿者（AI補完・既定値） |
|---|---|---|---|
| `p-20260807-002`（**次の本命候補**） | | | |
| `p-20260806-001`（承認済み・保留、優先度を下げる） | | | |
| `p-20260806-002`（保留） | | | |

## 24時間後実績記録（投稿翌日。**現在は暫定評価フェーズ中のスクショ運用**）

**現在はPhase 1の暫定評価フェーズ中。** X API半自動化（x-metrics-collector）はコードとして温存しているが、課金判断が下りるまで正式レーンとして起動しない（詳細: [provisional_evaluation_phase_2026-08-04.md](provisional_evaluation_phase_2026-08-04.md)）。人間は見えている数値のスクリーンショットを1枚渡すだけでよく、AIが見えた値だけこの表に記入する。**空欄＝未取得**（「未取得」と書き添える必要はない）。最小5項目: `impressions` / `likes` / `replies` / `profile_visits` / フォロワー純増数（参考）。`data_quality`は`manual`とし、`notes`に「スクショ確認ベース」と見えた範囲を残す。`link_clicks`等リンク関連はAIが常に`0`で補う（集客モードはリンクを使わないため実測ゼロ）。

**2026-08-06修正（記録先バグ）**: `data_quality`/`notes`は`ops/logs/metrics_snapshots.csv`（`schemas/metrics_snapshot.schema.json`準拠）には存在しないフィールドであり、書き込めない（`additionalProperties: false`）。**この表（`daily_brief.md`）を`data_quality`/`notes`付きの実績記録として使う。** `metrics_snapshots.csv`は引き続きschema準拠の数値のみのスナップショット（`data_quality`なし）として、完全なAPIデータが揃った場合の記録用に温存する。将来Google Sheets `metrics_24h`シートへ移行する場合は、そちらが正本になる（[gsheets_ledger_design_2026-08-03.md](gsheets_ledger_design_2026-08-03.md)参照）。

| post_id | impressions（人間） | likes（人間） | replies（人間） | profile_visits（人間） | フォロワー純増数・参考（人間） | data_quality（AI） | notes（AI） |
|---|---|---|---|---|---|---|---|
| （AI事前記入） | | | | | | | |

## 投稿案の競合比較記録（複数候補から1本を選んだ場合のみ。AI記入）

**2026-08-06追加**: `post_log.schema.json`に`notes`フィールドが存在しないため（2026-08-06の機能監査で判明）、複数候補を比較した際の競合比判定・採否理由はここに記録する（schema変更なし。`posted_url`等と同じく、schemaに格納場所がない情報を扱う欄）。詳細は[.claude/agents/logger.md](../../.claude/agents/logger.md)の「競合比判定の記録」参照。

| 対象日 | 採用post_id | 不採用post_id | hook_competitor_assessment（採用案） | whole_post_competitor_assessment（採用案） | strongest_axis | weakest_axis | why_selected | why_rejected（不採用案） |
|---|---|---|---|---|---|---|---|---|
| 2026-08-06 | `p-20260806-001` | `p-20260806-002`（保留・reject確定ではない） | 強い | 強い〜同等 | 緊張感 | 差別化（旧Day3候補と題材が近く要観察） | 他者視線・気まずさ・仕事文脈の仮説を維持しつつ、旧Day3候補より意味明瞭性を改善したと人間が判断（2026-08-06人間承認） | 案Bは保留(hold)。内面的な気づき寄りで停止力が案Aより弱いと人間が判断。棄却確定ではなく次点として保持 |
| 2026-08-06（判定確定日。作成は2026-08-05相当） | （なし） | `p-20260805-001`（旧Day3候補） | — | — | — | — | — | `human_rejected_meaning_unclear`。人間最終承認で意味不明と判定し不採用。未実測であることを理由にした復活は行わない、と人間が明示 |
| 2026-08-07（CTA別ルール運用テストサイクル。**人間最終判断確定**） | `p-20260807-002`（次の本命候補） | `p-20260807-001`（`observation only`として保持、reject確定ではない） | 強い | 強い | 緊張感／差別化／自分事化／停止力 | — | 5軸中複数軸で「強い」、`cta_fit_assessment`も強い。benchmark candidateとして次の本命候補に確定（2026-08-07人間承認） | `p-20260807-001`は1回修正後も`observation only`止まり。棄却ではなく観察対象として保持 |

## スキップ/持ち越し記録（AIが下書き、人間は確認のみ）

当日中に`approved`が出ない場合、mode-orchestratorが状況・理由・翌日の扱い案を自動で下書きする。人間はそれを確認し、必要なら1点だけ修正すればよい（ゼロから書かない）。

| 対象post_id | 状況（AI記入） | 理由（AI記入） | 翌日の扱い案（AI記入） | 人間の確認・修正（あれば） |
|---|---|---|---|---|
| | | | | |

## 懸念・要確認

- **人間最終審査待ち**: `p-20260807-002`（案D）について、意味が通るか／一文目で止まる理由があるか／過去実績より弱く見えないか／抽象的すぎないか／プロフィールへ飛ぶ理由が自然か、の最終確認をお願いします。承認後、実投稿（人間の操作）→投稿URL記入へ進みます
- 案A（`p-20260806-001`）・案B（`p-20260806-002`）は承認済みのまま保留。今回は新しい案を増やさず、分岐を閉じる方針のため、これ以上の追加検討はしていません
- 旧Day3候補（`p-20260805-001`）は失敗作として確定・クローズ済み
- **次の実測タイミング**: 案Dの投稿・24時間後実測が取れ次第、これがCTA別強さ判定ルール（Cold-start mode）下での**初回の実測サンプル**となる。この1件で同条件群のサンプルが0→1件になるのみで、`Relative benchmark mode`への切り替え（5件以上）にはまだ遠い
- Day1（`p-20260803-001`）・Day2（`p-20260804-002`）の実測データが、`post_log.jsonl`/`metrics_snapshots.csv`/この表のいずれにも未反映のまま（既知のギャップ。別途対応が必要。今回は修理しない）
- `daily_brief.md`の単一スナップショット構造（対象日が1つしか持てない）は認識済みの既知課題だが、今回は修理しない（OS修理より運用前進を優先する方針のため）
